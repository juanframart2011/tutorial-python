def sumar( *arg ):
    sumar = arg[0]

    for value in arg[1:]:
        sumar += value

    return sumar

def restar( *arg ):
    restar = arg[0]

    for value in arg[1:]:
        restar -= value

    return restar

def multiplicar( *arg ):
    sumar = arg[0]

    for value in arg[1:]:
        multiplica *= value

    return multiplica
